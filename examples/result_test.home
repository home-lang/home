// Test Result<T, E> type and error propagation
fn divide(a: int, b: int): int {
    if (b == 0) {
        return 0  // In a real Result type, this would be Err
    }
    return a / b
}

let x = divide(10, 2)  // 5
let y = divide(10, 0)  // Error case

print("Result:", x)
