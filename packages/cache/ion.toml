[package]
name = "ion-cache"
version = "0.1.0"
authors = ["Ion Contributors"]
description = "Ion Cache System - IR caching for faster compilation"
license = "MIT"

[dependencies]
# Cache utilities

[scripts]
test = "zig test src/ir_cache.zig"
