# Bun Immutable String Source Slice

Upstream: `/Users/chrisbreuer/Code/bun`, SHA
`fd0b6f1a271fca0b8124b69f230b100f4d636af6`.

The Unicode, grapheme, visibility, exact-size matcher, and HTML escaping leaves
are copied from Bun `src/bun_core/string/immutable/`. `paths.zig` is copied from
Bun `src/paths/string_paths.zig`, which provides the path helper surface
re-exported by Bun `src/string/immutable.zig`.
