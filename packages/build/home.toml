[package]
name = "home-build"
version = "0.1.0"
authors = ["Home Contributors"]
description = "Home Build System - Parallel builds and watch mode"
license = "MIT"

[dependencies]
# Build system utilities

[scripts]
test = "zig test src/parallel_build.zig"
