[package]
name = "ion-build"
version = "0.1.0"
authors = ["Ion Contributors"]
description = "Ion Build System - Parallel builds and watch mode"
license = "MIT"

[dependencies]
# Build system utilities

[scripts]
test = "zig test src/parallel_build.zig"
