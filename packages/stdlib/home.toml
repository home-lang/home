[package]
name = "home-stdlib"
version = "0.1.0"
authors = ["Home Contributors"]
description = "Home Standard Library - Built-in modules and functions"
license = "MIT"

[dependencies]
home-ast = { path = "../ast" }
home-types = { path = "../types" }

[scripts]
test = "zig test src/stdlib.zig"
