name: CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Use cached node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            node-modules-

      - name: Install Dependencies
        run: bun install

      - name: Lint
        run: bun run lint

  typecheck:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Use cached node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            node-modules-

      - name: Install Dependencies
        run: bun install

      - name: Typecheck
        run: bun --bun run typecheck

  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Use cached node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            node-modules-

      - name: Install Dependencies
        run: bun install

      - name: Unit Test
        run: bun test

  build-and-test-home:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64
            platform: linux
          - os: ubuntu-latest
            arch: aarch64
            platform: linux
          - os: macos-latest
            arch: x86_64
            platform: macos
          - os: macos-latest
            arch: aarch64
            platform: macos
          - os: windows-latest
            arch: x86_64
            platform: windows

    steps:
      - uses: actions/checkout@v5

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: '0.17.0-dev.56+a8226cd53'

      - name: Verify Zig Installation
        run: zig version

      - name: Build Home Compiler (Native)
        if: matrix.arch == 'x86_64' || (matrix.os == 'macos-latest' && matrix.arch == 'aarch64')
        run: zig build

      - name: Build Home Compiler (Cross-compile)
        if: matrix.arch == 'aarch64' && matrix.os != 'macos-latest'
        run: zig build -Dtarget=${{ matrix.arch }}-${{ matrix.platform }}

      - name: Run Zig Unit Tests (Native only)
        if: matrix.arch == 'x86_64' || (matrix.os == 'macos-latest' && matrix.arch == 'aarch64')
        run: zig build test --summary all

      - name: Run Diagnostic Snapshot Tests (Native only)
        if: matrix.arch == 'x86_64' || (matrix.os == 'macos-latest' && matrix.arch == 'aarch64')
        run: zig build test-diagnostics --summary all

      - name: Run Integration Tests (Unix)
        if: (matrix.arch == 'x86_64' || (matrix.os == 'macos-latest' && matrix.arch == 'aarch64')) && matrix.platform != 'windows'
        run: |
          chmod +x tests/integration/test_runner.sh
          ./tests/integration/test_runner.sh

      - name: Run Integration Tests (Windows)
        if: matrix.platform == 'windows' && matrix.arch == 'x86_64'
        shell: bash
        run: |
          chmod +x tests/integration/test_runner.sh
          ./tests/integration/test_runner.sh

      - name: Test Native Codegen (Unix)
        if: (matrix.arch == 'x86_64' || (matrix.os == 'macos-latest' && matrix.arch == 'aarch64')) && matrix.platform != 'windows'
        run: |
          ./zig-out/bin/home parse tests/integration/11_ternary_advanced.home
          ./zig-out/bin/home parse tests/integration/17_switch_advanced.home
          ./zig-out/bin/home parse tests/integration/20_combined_features.home

      - name: Test Native Codegen (Windows)
        if: matrix.platform == 'windows' && matrix.arch == 'x86_64'
        shell: bash
        run: |
          ./zig-out/bin/home.exe parse tests/integration/11_ternary_advanced.home
          ./zig-out/bin/home.exe parse tests/integration/17_switch_advanced.home
          ./zig-out/bin/home.exe parse tests/integration/20_combined_features.home

      - name: Upload Home Binary (Unix)
        if: matrix.platform != 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: home-${{ matrix.platform }}-${{ matrix.arch }}
          path: zig-out/bin/home
          retention-days: 7

      - name: Upload Home Binary (Windows)
        if: matrix.platform == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: home-${{ matrix.platform }}-${{ matrix.arch }}
          path: zig-out/bin/home.exe
          retention-days: 7

  # =========================================================================
  # Kernel Tests
  # =========================================================================
  test-kernel:
    name: Kernel Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: '0.17.0-dev.56+a8226cd53'

      - name: Run kernel memory tests
        run: |
          zig build-exe --name test_memory --dep testing --dep memory \
            -Mroot=packages/kernel/tests/test_memory.zig \
            -Mtesting=packages/testing/src/modern_test.zig \
            -Mmemory=packages/kernel/src/memory.zig
          ./test_memory

      - name: Run kernel paging tests
        run: |
          zig build-exe --name test_paging --dep testing \
            -Mroot=packages/kernel/tests/test_paging.zig \
            -Mtesting=packages/testing/src/modern_test.zig
          ./test_paging

      - name: Run scheduler tests
        run: |
          zig build-exe --name test_scheduler --dep testing \
            -Mroot=packages/kernel/tests/test_scheduler.zig \
            -Mtesting=packages/testing/src/modern_test.zig
          ./test_scheduler

      - name: Run integration tests
        run: |
          zig build-exe --name test_integration --dep testing \
            -Mroot=packages/kernel/tests/test_integration.zig \
            -Mtesting=packages/testing/src/modern_test.zig
          ./test_integration

  # =========================================================================
  # Fuzzing (issue #10)
  # =========================================================================
  #
  # Short, mutation-based fuzz session against the lexer and parser on
  # every PR. The harness is subprocess-isolated so the known parser
  # hang (issue #16) doesn't wedge CI — timeouts are logged as findings
  # but do not fail the build. Only actual crashes (signals, unexpected
  # exit codes) fail this job.
  #
  # Long-running fuzzing belongs in fuzz-nightly.yml, not here — CI
  # minutes are real and a 60s budget is enough to catch obvious
  # regressions without slowing down PR feedback.
  fuzz:
    name: Fuzz (lex + parse, 60s/target)
    runs-on: ubuntu-latest
    needs: build-and-test-home
    steps:
      - uses: actions/checkout@v5

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: '0.17.0-dev.56+a8226cd53'

      - name: Build Home Compiler
        run: zig build

      - name: Fuzz lexer (60s)
        run: zig build fuzz-lexer -- --seconds 60 --findings fuzz-findings

      - name: Fuzz parser (60s)
        run: zig build fuzz-parser -- --seconds 60 --findings fuzz-findings

      - name: Upload fuzz findings
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-findings-${{ github.run_id }}
          path: fuzz-findings/
          if-no-files-found: ignore
          retention-days: 14

  test-filesystem:
    name: Filesystem Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: '0.17.0-dev.56+a8226cd53'

      - name: Run filesystem tests
        run: |
          zig build-exe --name test_filesystems --dep testing \
            -Mroot=packages/fs/tests/test_filesystems.zig \
            -Mtesting=packages/testing/src/modern_test.zig
          ./test_filesystems

  test-network:
    name: Network Stack Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: '0.17.0-dev.56+a8226cd53'

      - name: Run network stack tests
        run: |
          zig build-exe --name test_network --dep testing \
            -Mroot=packages/net/tests/test_network.zig \
            -Mtesting=packages/testing/src/modern_test.zig
          ./test_network
