// Test function declarations and calls
fn add(a: int, b: int) -> int {
    return a + b;
}

fn multiply(x: int, y: int) -> int {
    return x * y;
}

let result1 = add(5, 3);
let result2 = multiply(4, 7);
let combined = add(result1, result2);
